class Solution:
    def uniformArray(self, nums1):
        odd = [x for x in nums1 if x % 2 != 0]

        if not odd:
            return True

        minodd = min(odd)

        for x in nums1:
            if x % 2 == 0 and x <= minodd:
                return False

        return True
